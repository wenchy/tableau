syntax = "proto3";
package test;
option go_package = "github.com/Wenchy/tableau/testpb";

enum FieldCardinality
{
    FIELD_CARDINALITY_REQUIRED = 0; // appears exactly one time
    FIELD_CARDINALITY_OPTIONAL = 1; // appears zero or one times
    FIELD_CARDINALITY_REPEATED = 2; // appears zero or more times
}